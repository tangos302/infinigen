"""Bundled CC0 GLB model dump for the LoadedGltfFactory pipeline.

Source: Kenney CC0 packs (kenney.nl) mirrored on OpenGameArt:

  - medieval/  : Retro Medieval Kit (105 GLBs — buildings, walls,
                 towers, docks, columns, props)
  - nature/    : Nature Kit 2.1 (329 GLBs — trees, rocks, plants,
                 mushrooms, flowers, cliffs, fences)

License: CC0. No attribution required, unrestricted commercial use.
LICENSE.txt files preserved inside each pack's source zip; reproduce
in shipped builds if your local jurisdiction needs it.

Adding more packs:

  1. Curl a CC0 GLB pack (Kenney via OpenGameArt is the easiest source).
  2. Extract its GLB files into ``runtime/models/<category>/``.
  3. They're auto-registered by ``LoadedGltfFactory`` on next import.
"""
